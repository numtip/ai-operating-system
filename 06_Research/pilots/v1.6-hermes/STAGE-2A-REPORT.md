# STAGE 2A — Cost Observability + Budget-Stop Evidence

**Date:** 2026-08-10
**Branch:** `evidence/stage-2a-cost-controls`
**Baseline:** `origin/main @ 7a05f709`
**Verdict:** `STAGE_2A_PARTIAL`

---

## Summary

Minimal, deterministic cost-telemetry + budget-guard controls were added to the
AI-OS repo (no LLM, no network, no secrets). The 3-comparable-run protocol was
executed in **MOCK mode** with deterministic stop behavior. A **LIVE run was NOT
performed** because no existing configured Hermes + API key is present and creating
or editing any secret (`.env` / key) is forbidden by policy — the human-confirmation
gate was therefore not granted.

## Implementation

| File | Purpose |
|------|---------|
| `prompt-compiler/runtime/Cost-Telemetry.ps1` | run ID, timestamp, provider/model, token usage, estimated cost, budget guard (preflight + in-run), redaction, fingerprint, billing variance |
| `prompt-compiler/runtime/rate-table.json` | per-MTok rates marked `UNVERIFIED_RATE`; `verified: false` |
| `prompt-compiler/fixtures/mock-provider-response.json` | synthetic provider usage for tests/mock evidence |
| `prompt-compiler/tests/run-tests-stage2a.ps1` | 32 deterministic tests |
| `06_Research/pilots/v1.6-hermes/STAGE-2A-RUNBOOK.md` | comparable-run protocol + live-run gate + human checklist |
| `06_Research/pilots/v1.6-hermes/schemas/run-evidence.schema.json` | evidence JSON schema (3 runs) |
| `06_Research/pilots/v1.6-hermes/STAGE-2A-MOCK-EVIDENCE.json` | 3-run MOCK evidence (generated by the module) |
| `11_Templates/BILLING_RECONCILIATION_TEMPLATE.md` | local-vs-billing variance template (human-entered) |

### Required Stage 2A controls — status

| Control | Status |
|---------|--------|
| a. run ID + timestamp + provider/model + input/output/total tokens | ✅ `New-CostRunRecord` (tests 1–2) |
| b. estimated cost with assumptions + rate source | ✅ `Invoke-CostEstimate`; `UNVERIFIED_RATE` until rate verified (test 3) |
| c. per-run configurable budget cap (USD + tokens) | ✅ `Invoke-CostBudgetPreflight` / `Invoke-CostBudgetGuard` (test 4) |
| d. preflight + in-run stop `BUDGET_EXCEEDED` | ✅ boundary-exact tests; stop_result present (tests 5–6) |
| e. redaction — no secret / full prompt by default | ✅ `prompt_stored=false`, `raw_response_stored=false`, masking (test 7) |

## Test results

- `prompt-compiler/tests/run-tests-stage2a.ps1` — **32/32 PASS**
- Cap boundary coverage: `5a` exactly at USD cap → ALLOW; `5b` over → BUDGET_EXCEEDED;
  `5c/5d` same for token cap; `6a` record carries stop_result.
- `BUDGET_EXCEEDED` proven in mock evidence: cap `$0.02` < mock usage est `$0.0469`
  → all 3 comparable runs return `BUDGET_EXCEEDED` + `stop_result`, deterministic.

## 3-run protocol status

| Run | mode | status | reason |
|-----|------|--------|--------|
| 1 | MOCK | DONE | fixture usage, est $0.0469, stop BUDGET_EXCEEDED |
| 2 | MOCK | DONE | same fingerprint + cap, identical decision (comparability) |
| 3 | MOCK | DONE | identical |
| LIVE | LIVE | NOT_RUN | no configured Hermes+key without secret changes; human confirmation not granted |

**Status: `MOCK`** (LIVE blocked by policy — no existing configured runtime, secret creation forbidden).

## Billing reconciliation status

- Template created; `Test-CostBillingVariance` implemented (variance % vs tolerance).
- `STAGE-2A-MOCK-EVIDENCE.json` holds `billing_reconciliation` with `billed_usd = null`.
- **Missing human evidence:** real billed USD, verified rate table (`verified: true`),
  and filled variance fields. No rate/billing figures were invented.

## Gates

| Gate | Result |
|------|--------|
| `scripts/validate-indexes.ps1` | see below |
| `prompt-compiler/tests/run-tests.ps1` (existing 53) | see below |
| `prompt-compiler/tests/run-tests-stage2a.ps1` (new 32) | **PASS 32/32** |
| `scripts/check-bootstrap.ps1` | see below |
| `git diff --check` | see below |
| targeted secret scan (`main...HEAD`) | see below |

## Confirmed untouched

VPS, Docker, Cloudflare, DNS, production, publish, GOFFICE2026, Document Center,
RAE, M365/SharePoint, GitHub Pages — **none modified**. No Hermes install/upgrade,
no `.env` created/edited, no API key used or displayed. No commit/push to `main`.
